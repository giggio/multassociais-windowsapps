<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MultasSociais.WinStoreApp.Common">
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="StandardStyles.xaml"/>
    </ResourceDictionary.MergedDictionaries>
    <Style x:Key="MSLayoutRootStyle" TargetType="Grid">
		<Setter Property="Background" Value="White"/>
		<Setter Property="ChildrenTransitions">
			<Setter.Value>
				<TransitionCollection>
					<EntranceThemeTransition/>
				</TransitionCollection>
			</Setter.Value>
		</Setter>
	</Style>
    <Color x:Key="MSApplicationHeaderForegroundThemeBrush">Black</Color>
    <Style x:Key="MSPageHeaderTextStyle" TargetType="TextBlock" BasedOn="{StaticResource HeaderTextStyle}">
        <Setter Property="TextWrapping" Value="NoWrap"/>
		<Setter Property="VerticalAlignment" Value="Bottom"/>
		<Setter Property="Margin" Value="0,0,30,40"/>
        <Setter Property="Foreground" Value="{StaticResource MSApplicationHeaderForegroundThemeBrush}"/>
	</Style>
    <Style x:Key="MSTextPrimaryButtonStyle" TargetType="Button" BasedOn="{StaticResource TextButtonStyle}">
        <Setter Property="Foreground" Value="{StaticResource MSApplicationHeaderForegroundThemeBrush}"/>
	</Style>
</ResourceDictionary>